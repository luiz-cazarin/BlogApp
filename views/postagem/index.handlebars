
<div class="card mt-4">
    <div class="card-body">
<h1>{{postagem.titulo}}</h1>
        <hr>
        <small>Data da publicacao: {{postagem.data}}</small>
        <hr>
        <br>
        {{postagem.conteudo}}
    </div>
</div>